{
  "name": "vehicleflow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "pg": "^8.11.3"
  }
}